document.addEventListener('DOMContentLoaded',()=>{const params=new URLSearchParams(location.search);const q=params.get('q')||'';const input=document.querySelector('input[name=q],#siteSearch');if(input)input.value=q;const grid=document.querySelector('.products');const cards=[...document.querySelectorAll('[data-search]')];function applySearch(term){const query=(term||'').trim().toLowerCase();let n=0;if(cards.length){cards.forEach(c=>{const ok=!query||c.dataset.search.includes(query);c.style.display=ok?'':'none';if(ok)n++});const count=document.querySelector('#resultCount');if(count)count.textContent=n;const note=document.querySelector('.result-note');if(note){note.classList.toggle('active',!!query);note.textContent=query?`תוצאות חיפוש עבור “${term}” — מוצגים ${n} מוצרים מתוך הקטלוג לדוגמה.`:'';}if(grid)grid.classList.toggle('is-filtered',!!query)}}applySearch(q);if(input&&cards.length){input.addEventListener('input',e=>applySearch(e.target.value))}});