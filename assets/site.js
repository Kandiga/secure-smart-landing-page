document.addEventListener('DOMContentLoaded',()=>{const q=new URLSearchParams(location.search).get('q')||'';const input=document.querySelector('input[name=q],#siteSearch');if(input)input.value=q;const cards=[...document.querySelectorAll('[data-search]')];if(q&&cards.length){let n=0;cards.forEach(c=>{const ok=c.dataset.search.includes(q.toLowerCase());c.style.display=ok?'':'none';if(ok)n++});const count=document.querySelector('#resultCount');if(count)count.textContent=n}});